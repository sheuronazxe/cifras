services:
  cifras:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      # Configura tu dominio público para validar el origen del WebSocket
      # - ALLOWED_ORIGINS=https://tudominio.com
    restart: unless-stopped
